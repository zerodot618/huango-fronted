{
  "name": "huango-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "push-dir --dir=dist --branch=production --cleanup",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "pinia": "^2.0.23",
    "vue": "^3.2.37",
    "vue-router": "4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "push-dir": "^0.4.1",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vitest": "^0.24.3",
    "vue-tsc": "^0.40.4"
  }
}